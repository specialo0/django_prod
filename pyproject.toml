[tool.poetry]
name = "django-prod"
version = "0.1.0"
description = ""
authors = ["specialo0 <7000fffff@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
django = "^5.0.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
